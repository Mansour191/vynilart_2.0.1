# SQLite Database Configuration
# SQLite uses a local file, no connection parameters needed

# Database file will be created at: BASE_DIR/db.sqlite3
# No additional configuration required for SQLite

# Django settings already configured for SQLite in settings.py
